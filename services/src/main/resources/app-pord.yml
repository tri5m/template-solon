database:
  master:
    schema: xxx
    jdbcUrl: jdbc:mysql://localhost:xxx/xxx?useUnicode=true&characterEncoding=utf8&autoReconnect=true&rewriteBatchedStatements=true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: xxx
    password: xxx

mybatis:
  master:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    typeAliases:
      - "tk.mybatis.mapper.test.entity.*"
    mappers:
      - "classpath:mapper/*.xml"

    # tk mybatis的配置
    tk:
      mapper:
        style: camelhumpandlowercase
        safe-update: true
        safe-delete: true