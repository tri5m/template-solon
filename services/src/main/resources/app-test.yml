database:
  master:
    schema: test
    jdbcUrl: jdbc:mysql://172.29.91.45:3306/test?useUnicode=true&characterEncoding=utf8&autoReconnect=true&rewriteBatchedStatements=true
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456

mybatis:
  master:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    typeAliases:
      - "tk.mybatis.mapper.test.entity.*"
    mappers:
      - "classpath:mapper/*.xml"

    # tk mybatis的配置
    tk:
      mapper:
        style: camelhumpandlowercase
        safe-update: true
        safe-delete: true